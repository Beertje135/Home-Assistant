
alias: Licht - Smappee - Veranda - LED - Aan
sequence:
  - condition: template
    value_template: "{{ states('input_select.woning_status') not in ['Slapen'] }}"
  - condition: template
    value_template: "{{ states('switch.smappee_porch') in ['off'] }}"
  - service: switch.turn_on
    entity_id:
      - switch.smappee_porch
