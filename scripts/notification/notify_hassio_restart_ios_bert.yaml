
alias: Notify iOS Bert - Hassio - Restart
sequence:
  - condition: template
    value_template: "{{ states('input_boolean.notify_bert') in ['on'] }}"
  - service: notify.mobile_app_iphone_van_bert
    data_template:
      title: "SERVER RESTART"
      message: Ik heb mezelf opnieuw opgestart.
      data:
        push:
          thread-id: "server"
