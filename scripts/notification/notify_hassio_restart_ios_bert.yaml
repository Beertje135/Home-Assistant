
alias: Notify - Restart van Systeem - Ios Bert
sequence:
  - condition: template
    value_template: "{{ states('input_boolean.notify_bert') in ['on'] and states('person.bert') not in ['home'] }}"
  - service: notify.ios_ios_iphone_bert
    data_template:
      title: "SERVER RESTART"
      message: Ik heb mezelf opnieuw opgestart.
      data:
        push:
          thread-id: "server"
