
alias: Radio - Play Keuken Follow Badkamer
sequence:
  - service: media_player.play_media
    data_template:
      entity_id: media_player.kitchen_alexa_1
      media_content_type: TUNEIN
      media_content_id: "{{ states.media_player.bathroom_alexa.attributes.media_title }}"
  - delay: 00:00:05
  - service: media_player.volume_set
    data_template:
      entity_id: media_player.kitchen_alexa_1
      volume_level: >
        {% if (states.sensor.time.state) <= "08:30" and now().weekday() in (0,1,2,3,4) %}
          0.1
        {% elif ((states.sensor.time.state) <= "07:30") and now().weekday() in (5,6) %}
          0.1
        {% else %}
          0.3
        {% endif %}
